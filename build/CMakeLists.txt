cmake_minimum_required (VERSION 2.8)
project(kafkademo)
SET(CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS} "-std=c++11 -Wall -lrdkafka -lrdkafka++")
include_directories(../lib)
include_directories(/usr/local/include/librdkafka)
#slink_libraries(/usr/local/include/)
aux_source_directory(../src/producer DIR_SRCS_PRO)
aux_source_directory(../src/consumer DIR_SRCS_CON)
add_subdirectory(../lib lib.out)
add_executable(producer ${DIR_SRCS_PRO})
add_executable(consumer ${DIR_SRCS_CON})
target_link_libraries(producer producerhelper)
target_link_libraries(consumer consumerhelper)
SET(EXECUTABLE_OUTPUT_PATH ../bin)

#SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)